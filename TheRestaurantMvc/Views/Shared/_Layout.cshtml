@{
    var hasJwt = Context.Request.Cookies.ContainsKey("jsonWebToken");
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"] - The Restaurant</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>
</head>
<body>
<header>
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
        <div class="container-fluid">
            <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">The Restaurant</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse">
                <span class="navbar-toggler-icon"></span>
            </button>
    
            <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                <ul class="navbar-nav flex-grow-1 gap-3">
                    @if (hasJwt)
                    {
                        <li class="nav-item">
                            <a class="btn btn-outline-dark" asp-area="" asp-controller="MenuItems" asp-action="AdminIndex">Menu</a>
                        </li>

                        <li class="nav-item">
                            <a class="btn btn-outline-dark" asp-area="" asp-controller="MenuItems" asp-action="Index">Reservations</a>
                        </li>

                        <li class="nav-item">
                            <a class="btn btn-outline-dark" asp-area="" asp-controller="MenuItems" asp-action="Index">Tables</a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link text-dark" asp-area="" asp-controller="MenuItems" asp-action="Index">Menu</a>
                        </li>
                    }
                </ul>
                
                @if (hasJwt)
                {
                    <a class="btn btn-outline-primary btn-sm d-sm-none me-2" asp-controller="Auth" asp-action="Logout">Log Out</a>
                }
                else
                {
                    <a class="btn btn-outline-primary btn-sm d-sm-none me-2" asp-controller="Auth" asp-action="Login">Log In</a>

                }
            
                <div class="ms-auto me-3 d-none d-sm-block">
                    @if (hasJwt)
                    {
                        <a class="btn btn-outline-primary" asp-controller="Auth" asp-action="Logout">Log Out</a>
                    }
                    else
                    {
                        <a class="btn btn-outline-primary" asp-controller="Auth" asp-action="Login">Log In</a>
                    }
                </div>
            </div>
        </div>
    </nav>
</header>
<div class="container">
    <main role="main" class="pb-3">
        @RenderBody()
    </main>
</div>

<footer class="border-top footer text-muted">
    <div class="container">
        &copy; 2025 - TheRestaurantMvc
    </div>
</footer>
<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
<script src="~/js/site.js" asp-append-version="true"></script>
@await RenderSectionAsync("Scripts", required: false)
</body>
</html>